# Tasks

## 1. Spec and static output dir

- [x] 1.1 OpenSpec proposal, design, and mcp-gateway spec delta
- [x] 1.2 Root `public/.gitkeep`
- [x] 1.3 `vercel.json` `outputDirectory` is `"public"`; keep install/build, Fluid, rewrites, includeFiles

## 2. Proof and docs

- [x] 2.1 Tests: `outputDirectory` is `public`; `public/` exists; compiled entry still serves `/health` and Bearer initialize
- [x] 2.2 README notes the Output Directory
