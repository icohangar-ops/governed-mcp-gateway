# Tasks

## 1. Spec and catalog

- [x] 1.1 OpenSpec proposal, design, and mcp-gateway spec deltas
- [x] 1.2 Token heuristic + tax record types
- [x] 1.3 Tool catalog with named packs and synthetic oversized fixture

## 2. Gateway

- [x] 2.1 Session pack store (seed core + catalog; admit by need)
- [x] 2.2 `tools/list` pack / full modes + `_meta.cubiczan.tax`
- [x] 2.3 HMAC ledger events and `/v1/context/*` inspector
- [x] 2.4 `context.inspect` / `context.need` meta-tools
- [x] 2.5 Fail-closed: need ∩ allowlist; unauthenticated inspector rejected

## 3. Proof

- [x] 3.1 Tests: heuristic, flag oversized, pack vs full, denied need
- [x] 3.2 Gateway README section + fixture pointer
